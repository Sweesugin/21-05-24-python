def multiplication_table(n, m):
    print(f"Multiplication table for {n} up to {m} rows:")
    for i in range(1, m + 1):
        print(f"{n} x {i} = {n * i}")
num = int(input("Enter the number: "))
rows = int(input("Enter the number of rows: "))
multiplication_table(num, rows)
